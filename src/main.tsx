import React from "react";
import ReactDOM from "react-dom/client";
import SuspenceLoading from "./done/loading/SuspenseLoading";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SuspenceLoading />
  </React.StrictMode>
);
